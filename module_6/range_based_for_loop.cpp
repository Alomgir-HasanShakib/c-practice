#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    getline(cin, s);

    // shortcut  for loop  to pring single character from an string
    for (char c : s)
        cout << c << endl;

    cout << s << endl;
    return 0;
}